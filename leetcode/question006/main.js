/**
 * @param {string} s
 * @param {number} numRows
 * @return {string}
 */
var convert = function(s, numRows) {
    var matrix = [];
    for(var i=0;i<numRows;i++){
        matrix.push([]);
    }
    for(var i=0;i<s.length;){}
};